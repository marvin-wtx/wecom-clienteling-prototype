interface:
  display_name: WeCom Clienteling Prototype
  short_description: Build credible, operable WeCom clienteling prototypes.
  default_prompt: "Use $wecom-clienteling-prototype V4.0. Show and confirm the exact role, modules, second-level pages, page count, primary Journey, and depth before files. Keep brand-like mock values explicitly authorized and traceable. Build and browser-test the neutral mobile product first. Run a separate design intake, build representative screens from the unchanged executable UI kit and page recipes, pass the visible-Chrome layout probe, obtain user visual acceptance, extend the accepted design, and only then add review controls outside the phone."
